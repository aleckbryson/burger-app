<li>
	{{name}}

	<button class="change-eat btn btn-outline-dark" data-id="{{id}}" data-newhungry="{{eat}}">
		{{#if eat}}EAT TIME!{{else}}THROW UP!{{/if}}
	</button>

	<button class="delete-burger btn btn-outline-dark" data-id="{{this.id}}">DELETE!</button>
</li>
